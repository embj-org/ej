[package]
name = "lib-requests"
version = "0.1.0"
edition = "2024"

[dependencies]
ej = { path = "../../libs/lib-core" }
reqwest = { version = "0.12", features = ["json", "cookies"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lints]
workspace = true
